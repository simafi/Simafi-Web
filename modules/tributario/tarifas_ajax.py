from django.http import JsonResponse

def obtener_tarifas_escalonadas(request):
    """Vista para obtener tarifas escalonadas"""
    return JsonResponse({
        'exito': False,
        'mensaje': 'Función no implementada aún'
    })
















